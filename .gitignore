# Python
__pycache__/
*.py[cod]
.Python
.venv
venv/
*.egg-info/

# Django
*.log
db.sqlite3
/staticfiles/
/media/
local_settings.py

# Entorno y secretos — NUNCA en git
.env
.env.*
!.env.example
*.pem
*.key

# Tailwind (binario suelto, sin Node)
/tailwindcss
/tailwindcss-*

# Sistema
.DS_Store

# Evidencia generada (capturas, logs...): el repo de código es solo código. El registro de
# cada unidad, con su evidencia descrita, vive en el meta-repo (hallazgos.md), no aquí.
/evidencia/
