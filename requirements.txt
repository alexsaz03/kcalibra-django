# Dependencias del proyecto, versiones fijadas.
# Unidad 002 (esqueleto): Django, el conector de PostgreSQL (psycopg, con su rueda binaria) y
# django-template-partials.
Django==5.2.17
psycopg[binary]==3.3.4
django-template-partials==25.3
# Unidad 003 (cuentas y hogares): django-allauth resuelve el alta, la entrada, la salida y el
# límite de intentos fallidos — decidido por el curso (docs/01-constitucion/bias.md, regla 1a),
# no por preferencia propia. Sin el extra [socialaccount]: entrar con Google/Apple es la
# unidad R-21, fuera de alcance de esta.
django-allauth==65.19.2
